namespace Homework1;
class Program
{
    //The code used for Homework1
    static void Main(string[] args)
    {
       float x = 2.5F; // The value of x;
       float y = 3.3F; // The value of y;
       float z;
       z = (4*x*x)+3*y; // Creating the equation to find z
       Console.WriteLine("X = " + x + ", "+"Y = " + y);
       Console.WriteLine("The value of Z is: "+z);
    }
}
